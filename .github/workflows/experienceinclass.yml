name: Experience Reflection (inclass)
on:
  workflow_dispatch


jobs:
  createPullRequest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Make changes to pull request
      # TODO: modify to create template file
        run: |
          exptitle="experience-"$(date +"%Y-%m-%d")".md"
          cp .templates/experience-reflect.md experiences/$exptitle
          

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v5-beta
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: initialize experience
          committer: GitHub <noreply@github.com>
          author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          signoff: false
          base: main
          branch: experience
          branch-suffix: timestamp
          title: 'Experience Badge'
          body: |
            Checklist:
            - [ ] Merge prepare work into this PR
            - [ ] Link prepare issue to this PR
            - [ ] Complete experience reflection
            - [ ] Include name for matching to prismia
          team-reviewers: |
            instructors
